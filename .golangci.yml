version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - exhaustive # every ItemKind / CardState switch must be complete

settings:
  exhaustive:
    default-signifies-exhaustive: true

run:
  timeout: 5m
